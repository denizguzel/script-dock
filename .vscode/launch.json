{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Extension",
      "type": "extensionHost",
      "request": "launch",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}", "/home/denizguzel/development/Frontend/bstp-csr"],
      "outFiles": ["${workspaceFolder}/dist/**/*.js"],
      "preLaunchTask": "bun: build"
    }
  ]
}
